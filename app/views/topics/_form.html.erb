<%= bootstrap_form_for [@forum, @topic] do |f| %>
  <%= f.text_field :title %>

  <%= f.fields_for :posts, @post do |p| %>
    <%= p.text_area :body, rows: 2 %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
    <%= cancel_button path: [@forum, @topic] %>
  </div>
<% end %>
