<% content_for :page_header, @topic.title %>
<% breadcrumbize link_to(@forum.name, @forum), @topic.title %>

<%= new_topic_link %>

<div class="posts">
  <%= render @posts %>
</div>

<%= new_topic_link %>
