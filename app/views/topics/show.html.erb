<% content_for :page_header, @topic.title %>
<% breadcrumbize do
  [link_to(@forum.name, @forum), @topic.title]
end %>

<%= new_topic_link %>

<div class="posts">
  <%= render @posts %>
</div>

<%= new_topic_link %>
