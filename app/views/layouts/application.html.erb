<!DOCTYPE html>
<html lang="en">
<head>
  <title>Symposium</title>
  
  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
  
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="topbar">
      <div class="fill">
        <div class="container">
          <a class="brand" href="#">Project name</a>

          <ul class="nav">
            <li class="active"><a href="#">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
          </ul>

          <% if current_user %>
            <div class="pull-right">
              <p>Logged in as: <%= current_user.username %> <%= link_to "Log Out", logout_path %></p>
            </div>

          <% else %>
            <%= form_tag sessions_path, class: "pull-right" do %>
              <%= link_to "Create a New Account", signup_path %> or
              <%= text_field_tag :username, params[:username], placeholder: "Username", class: "input-small" %>
              <%= password_field_tag :password, params[:password], placeholder: "Password", class: "input-small" %>
              <button class="btn" type="submit">Sign in</button>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="container">

      <div class="content">

        <% flash.each do |type, message| %>
          <div class="alert-message <%= type %>">
            <%= message %>
          </div>
        <% end %>

        <% if content_for? :page_header %>
          <div class="page-header">
            <h1><%= yield(:page_header) %></h1>
          </div>
        <% end %>

        <%= yield %>
      </div>

    </div> <!-- /container -->


</body>
</html>
