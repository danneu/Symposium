<!DOCTYPE html>
<html lang="en">
<head>
  <title>Symposium</title>
  
  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
  
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="topbar">
      <div class="fill">
        <div class="container">
          <a class="brand" href="/">Symposium</a>

          <ul class="nav">
            <li class="active"><a href="#">Home</a></li>
            <li><%= link_to "Members", users_path %></li>
          </ul>

          <% if current_user %>
            <div class="userbox pull-right">
              <p><span class="username">Welcome, <%= profile_for current_user %></span> <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "logout" %></p>
            </div>

          <% else %>
            <%= form_tag user_session_path, method: :post, class: "pull-right" do %>
              <%= link_to "Sign Up", new_user_registration_path %> or
              <%= text_field_tag :username, params[:username], placeholder: "Username", class: "input-small" %>
              <%= password_field_tag :password, params[:password], placeholder: "Password", class: "input-small" %>
              <button class="btn" type="submit">Sign in</button>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="container">

      <div class="content">
        <%= flash_messages %>
        <%= breadcrumbs %>

        <% if content_for? :page_header %>
          <div class="page-header">
            <h1><%= yield(:page_header) %></h1>
          </div>
        <% end %>

        <%= yield %>

      </div>

      <footer><p>Symposium &copy; 2012</p></footer>

    </div> <!-- /container -->


</body>
</html>
